import { CommentResponseInterceptor } from './comment-response.interceptor';

describe('CommentResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new CommentResponseInterceptor()).toBeDefined();
  });
});
